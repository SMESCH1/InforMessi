# Dockerfile.heartbeat - Servidor de heartbeat
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir flask requests

COPY scripts/heartbeat-server.py .
RUN mkdir -p /app/data

EXPOSE 8080

CMD ["python3", "heartbeat-server.py"]

